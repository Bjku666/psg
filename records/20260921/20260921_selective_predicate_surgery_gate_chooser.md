# Selective Predicate Surgery v1 — Gate × Chooser qualification

日期：2026-09-21（Asia/Shanghai）  
Carrier：`results/relation_decision_regret_v1/p14_full_carrier/{fit_full,dev_full}.pkl`  
Support：frozen Top-50 physical pairs，depth=3，56 predicates

## Pre-flight

- p1.7 remains immutable; its Gate-0 exactness and MEAR failure were reused as
  qualification context, not overwritten.
- Full-dev Gate-0 reproduced: native mR=`0.59108448`, exact oracle mR=`0.81461290`,
  headroom=`+22.35284 mR pp`; additivity/parity residuals `<1.3e-16`.
- No GPU was used by this diagnostic; unrelated GPU training jobs were left intact.
- Features were generated by `row_features.py`; teacher-only fields (`relations`,
  `gt_pair`, utilities and labels) were removed before model fitting.
- Fit groups were deterministically split 90/10 by `bootstrap_group`. To keep the
  CPU qualification bounded, logistic Gate fitting used a fixed 200,000-row sample
  and chooser Ridge used all positive rows up to a fixed 100,000-row cap. Calibration
  and dev population evaluation remained complete.

## Results

| diagnostic | dev mR | ΔmR vs native |
|---|---:|---:|
| native | 0.59108448 | 0.000 pp |
| exact Gate + exact Chooser | 0.81461290 | +22.35284 pp |
| learned Gate + exact Chooser | 0.52778741 | **−6.32971 pp** |
| exact Gate + learned Chooser | 0.74004135 | **+14.89569 pp** |

The chooser recovers `14.89569 / 22.35284 = 66.64%` of oracle headroom, above
the preregistered 50% chooser gate. The learned Gate fails the required
`+4.4706 mR pp` (20% of oracle headroom) by a wide margin. Fit calibration selected
q=1% (threshold `0.91067914`), with calibration ΔmR=`−0.23621 pp` and ΔR=`+0.32715 pp`.

## Decision

**STOP / KILL current DASP Gate formulation.** The observable representation is
adequate for EDIT-to-WHICH, but this risk-aware row Gate is not learnable under
the locked fit→calibration→dev protocol. Do not run DASP seed0, extra seeds,
confirm, official test, or visual Candidate B from this result. Any continuation
must register a new Gate formulation/control (e.g. calibrated selective-risk
model) without retuning the dev split.

Machine-readable output: `results/selective_predicate_surgery_v1/gate_chooser_full_dev.json`.

